import React from 'react';
import { shallow } from 'enzyme';
import OrLine from '.';

describe('OrLine', () => {
  it('should test or and OrLine component', () => {
    shallow(<OrLine />);
  });
});
