import React from 'react';
import { shallow } from 'enzyme';
import SwitchToSignupOrLogin from '.';

describe('LoginSvg', () => {
  it('should test or and LoginSvg component', () => {
    shallow(<SwitchToSignupOrLogin />);
  });
});
