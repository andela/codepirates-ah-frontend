import React from 'react';
import { shallow } from 'enzyme';
import SwitchToSignupOrLogin from '.';

describe('SignupSvg', () => {
  it('should test signupSvg component', () => {
    shallow(<SwitchToSignupOrLogin />);
  });
});
